services:
  frontend:
    build: .
    environment:
      NEXT_PUBLIC_API_BASE_URL: http://localhost:8000/api/v1
    ports:
      - "3000:3000"
    command: npm run dev
    volumes:
      - .:/app
      - node-modules:/app/node_modules

volumes:
  node-modules:
